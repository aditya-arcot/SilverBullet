services:
    silverbullet:
        image: ghcr.io/silverbulletmd/silverbullet:latest
        container_name: silverbullet
        restart: unless-stopped

        environment:
            - SB_USER=${USERNAME?}:${PASSWORD?}
        networks:
            - traefik-public
        volumes:
            - ./space:/space

        labels:
            - traefik.enable=true
            - traefik.prod=true
            - traefik.http.routers.silverbullet.rule=Host(`silverbullet.aarcot.com`)
            - traefik.http.routers.silverbullet.entrypoints=websecure

networks:
    traefik-public:
        external: true
